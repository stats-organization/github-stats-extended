{
  "extends": ["../../tsconfig.base.json"],
  "include": ["src", "vitest.config.ts"],
  "compilerOptions": {
    // package is contain lot of errors no need to perform typecheck now
    "noCheck": true,
    "allowJs": true,
    "checkJs": true,
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "outDir": "build"
  }
}
