{
  "name": "github-readme-stats-backend",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": "24.x"
  },
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "vitest",
    "test:update:snapshot": "vitest -u",
    "test:e2e": "vitest --config vitest.config.e2e.ts",
    "bench": "vitest bench --run --config vitest.config.bench.ts"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "catalog:default",
    "axios-mock-adapter": "^2.1.0",
    "express": "^5.2.1",
    "jsdom": "28.1.0",
    "vitest": "catalog:default"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "github-readme-stats-core": "workspace:^",
    "pg": "^8.18.0"
  }
}
