{
  "extends": "./tsconfig.base.json",
  "include": ["scripts"],
  "compilerOptions": {
    "customConditions": [],
    // Base config sets `types: []`; these scripts run on node
    "types": ["node"]
  }
}
