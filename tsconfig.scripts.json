{
  "extends": "./tsconfig.base.json",
  "include": ["scripts"],
  "compilerOptions": {
    "noEmit": true,
    "customConditions": [],
    // Base config sets `types: []`; these scripts run on node
    "types": ["node"]
  }
}
